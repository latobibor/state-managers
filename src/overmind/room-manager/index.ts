import { roomManagerState } from './room-manager.state.ts';
import * as roomManagerActions from './room-manager.actions.ts';

export const roomManagerConfig = {
  state: roomManagerState,
  actions: roomManagerActions
};
